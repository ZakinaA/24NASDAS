<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Welcome!{% endblock %}</title>
        {% block stylesheets %}{% endblock %}
    </head>
    <body>
        {% block body %}{% endblock %}
        {% block javascripts %}{% endblock %}
		
		LISTE DES COURS</br>
		<table>
		{% for c in pCours %}
		<tr>
			<td><a href="{{ path('app_cours_consulter', { 'id': c.id }) }}">{{ c.libelle }}</a></td>
	{% else %}
		<tr>
		<td>Aucun cours n'a été trouvé.</td>
		</tr>
	{% endfor %}
</table>
	<div class="bouton">
    <a href="{{ path('app_cours_ajouter') }}" style="
        display: inline-block;
        background-color: black;
        color: white;
        padding: 10px 10px;
        text-decoration: none;
        border-radius: 5px;
        font-size: 16px;
        text-align: center;
        cursor: pointer;" >
        Ajouter un cours
    </a>
</div>

	
	 
    </body>
</html>